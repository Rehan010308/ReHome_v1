# Supabase (browser) — never put the service-role key here
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: set on the Edge Function `analyze-item`, not in Vite
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=
