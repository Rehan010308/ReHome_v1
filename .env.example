# ReHome_v1 environment variables
# Copy this file to `.env` and fill in real values.
# NEVER commit the real `.env` file. NEVER put service-role keys in the frontend.

# Supabase Auth (Phase 2)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
